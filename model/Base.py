
from sqlalchemy.orm import DeclarativeBase

class Base(DeclarativeBase):
    """
    Base class to inherit from. Needed to make our models for the ORM by the docuementary for SQLAlchemy.
    """
    pass